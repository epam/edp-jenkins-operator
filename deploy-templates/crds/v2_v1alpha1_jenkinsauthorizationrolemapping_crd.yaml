apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: jenkinsauthorizationrolemappings.v2.edp.epam.com
spec:
  group: v2.edp.epam.com
  names:
    kind: JenkinsAuthorizationRoleMapping
    listKind: JenkinsAuthorizationRoleMappingList
    plural: jenkinsauthorizationrolemappings
    singular: jenkinsauthorizationrolemapping
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                  of an object. Servers should convert recognized schemas to the latest
                  internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
                  object represents. Servers may infer this from the endpoint the client
                  submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              properties:
                ownerName:
                  type: string
                  nullable: true
                  description: 'Property indicates which jenkins car should be taken to initialize correct client.'
                group:
                  type: string
                  description: 'mapping group'
                roleType:
                  type: string
                  description: 'role type, available options: globalRoles, projectRoles, nodeRoles'
                roles:
                  type: array
                  items:
                    type: string
              required:
                - roleType
                - roles
                - group
              type: object
            status:
              type: object
              additionalProperties: true
      subresources:
        status: {}